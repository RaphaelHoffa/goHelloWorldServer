apiVersion: v1
kind: Service
metadata:
  name: raphael-service
  namespace: raphael-namespace
spec:
  selector:
    app: raphael-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP